class Unit {
  constructor(name, aliases, baseName, relative, category) {
    this.name = name;
    this.aliases = aliases;
    this.baseName = baseName;
    this.relative = relative;
    this.category = category;
  }
}

module.exports = Unit;
